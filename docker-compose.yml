services:
  bot:
    build: .
    image: xantheshop-bot
    env_file: .env
    environment:
      # Абсолютный путь к БД в volume (4 слэша: scheme + "//" + "/data/...")
      DATABASE_URL: sqlite+aiosqlite:////data/shop.db
    volumes:
      - botdata:/data
    restart: unless-stopped

volumes:
  botdata:
